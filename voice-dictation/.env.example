# どちらかの方法でキーを渡せます。
#   (A) この .env を使う（npm start 時に手元のシェルで export しておく）
#   (B) アプリの config.json に直接書く（推奨。メニュー「設定ファイルを開く」から編集）

# 文字起こし（OpenAI Whisper など）
OPENAI_API_KEY=sk-...

# Claude 整形
ANTHROPIC_API_KEY=sk-ant-...
